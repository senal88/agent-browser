interface:
  display_name: "Agent Browser"
  short_description: "Repo-specific patterns and workflows for agent-browser"
  default_prompt: "Use the agent-browser repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true